# Virtual Network Link Defaults Overrides Example

This example shows how to override the default settings for Virtual Network Links created for Private DNS Zones linked to Virtual Networks. It demonstrates how to customize the names and resolution policies of the default links using the `virtual_network_link_default_overrides` variable.
